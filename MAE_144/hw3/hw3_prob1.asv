clear all; clc; clf;

s = tf('s');
a0 = 0.1;
b0 = 0.1;
d = 6;

num = b0;
den = 

num_delay = b0 * (1 - d*s/2 + (d*2)^2/12);
den_delay = (s + a0) * (1 + d*s/2 + (d*s)^2/12);
G_delay = num_delay / den_delay;
figure(1)
bode(G);